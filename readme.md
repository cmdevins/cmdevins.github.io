# Time Travelers Pokemon League Website

## Development

Run the website locally in development mode.

```sh
npm run dev
```

## Build and Deploy

Run the build script to create the static assets in `/dist`.

```shell
npm run build
```

You can preview the build locally before committing.

```shell
npm run preview
```
