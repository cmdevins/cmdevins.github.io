{
  "name": "ttpkmn-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format-check": "prettier ./**/*.{js,json,md} --check",
    "format-fix": "prettier ./**/*.{js,json,md} --write"
  },
  "dependencies": {
    "prettier": "^3.9.9"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
